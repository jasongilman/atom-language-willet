'use babel';

import AtomLanguageWilletView from '../lib/atom-language-willet-view';

describe('AtomLanguageWilletView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
